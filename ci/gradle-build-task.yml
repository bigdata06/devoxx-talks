---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: alpine
    tag: '3.5'

inputs:
- name: devoxx-talks-src

run:
  path: sh
  dir: devoxx-talks-src
  args:
  - -exc
  - |
    chmod 755 gradlew
    ./gradlew --stacktrace build
