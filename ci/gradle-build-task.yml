---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: java
    tag: '8-jdk-alpine'

inputs:
- name: devoxx-talks-src

run:
  path: sh
  dir: devoxx-talks-src
  args:
  - -exc
  - |
    chmod 755 gradlew
    ./gradlew --stacktrace build
